# FIN-P3-INFO-GAIN-06：端到端与确定性测试

本任务从冻结的 03A、03B、03C、04 输出组装 05 汇总，并执行固定的 14 例 bad-data Gate。测试仅验证端到端状态、黄金指纹、确定性序列化、非生产状态和失败记录保留；不会重算 M/F/R、共同样本、组合或信息增益。

黄金快照固定汇总与 Gate 的输出指纹。任何指纹、轨道状态、坏数据处理、PIT/共同样本/契约阻断语义漂移都必须使测试失败。
